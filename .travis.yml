language: python
python:
  - "3.9"

services:
  - postgresql

branches:
  only:
    - main

jobs:
  include:
  - name: "Python on Windows"
    os: windows
    language: shell

script: python -m unittest -v

notifications:
  email:
    recipients:
      - iryna.yarka@gmail.com
